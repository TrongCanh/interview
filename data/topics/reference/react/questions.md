# React Interview Questions / Câu hỏi Phỏng vấn React

## 📚 Cơ bản / Basics

1. React là gì? Tại sao dùng React?

2. Virtual DOM là gì? Nó hoạt động như thế nào?

3. JSX là gì?

4. Sự khác biệt giữa React Element và React Component?

5. React là library hay framework? Tại sao?

6. Declarative vs Imperative programming trong React?

## 🎣 React Hooks - Deep Dive

7. `useState` hoạt động như thế nào bên trong (batching, re-render)? Cách dùng? Ví dụ?

8. `useEffect` hoạt động như thế nào? Sự khác biệt giữa `useEffect`, `useLayoutEffect`, và `useInsertionEffect`? Cách dùng? Ví dụ?

9. `useContext` hoạt động như thế nào? Khi nào nên dùng? Cách dùng? Ví dụ?

10. `useReducer` hoạt động như thế nào? Khi nào nên dùng `useReducer` thay vì `useState`? Cách dùng? Ví dụ?

11. `useMemo` và `useCallback` khác nhau như thế nào? Khi nào nên dùng? Cách dùng? Ví dụ?

12. `useId` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

13. `useImperativeHandle` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

14. `useDebugValue` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

15. `useSyncExternalStore` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

16. `useTransition` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

17. `useDeferredValue` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

18. `useOptimistic` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

19. `useActionState` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

20. `useEffectEvent` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

## 🧩 React Components - Deep Dive

21. `<Fragment>` (<> ... </>) là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

22. `<Profiler>` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

23. `<StrictMode>` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

24. `<Suspense>` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

25. `<Activity>` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

26. `<ViewTransition>` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

## 🛠 React APIs - Deep Dive

27. `createContext` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

28. `memo` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

29. `lazy` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

30. `startTransition` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

31. `use` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

32. `act` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

33. `cache` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

34. `cacheSignal` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

35. `addTransitionType` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

36. `captureOwnerStack` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

37. `experimental_taintObjectReference` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

38. `experimental_taintUniqueValue` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

## 📋 React Directives - Deep Dive

39. `server-jsx` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

## 🚨 React Compiler & ESLint

40. React Compiler là gì? Nó hoạt động như thế nào? Cách dùng? Ví dụ?

41. ESLint Plugin: React Hooks là gì? Các quy tắc hữu ích nhất? Cách dùng? Ví dụ?

## 📏 Rules of React

42. Components & Hooks phải pure là gì? Tại sao quan trọng?

43. React tự gọi Components & Hooks là gì?

44. Quy tắc dùng Hooks đúng chỗ là gì? Cách tránh vi phạm?

## 🕰 Legacy APIs

45. `Children` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

46. `cloneElement` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

47. `Component base class` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

48. `createElement` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

49. `createRef` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

50. `forwardRef` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

51. `isValidElement` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?

52. `PureComponent` là gì? Khi nào nên dùng? Cách dùng? Ví dụ?
